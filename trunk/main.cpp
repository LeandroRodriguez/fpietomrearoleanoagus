#include <iostream>
#include "ManejoArchivos/Bloque.h"
#include "ManejoArchivos/RegistroVariable.h"
#include "Constantes.h"

using namespace std;

int main()
{

    cout << "sisi funca" << endl;
    //Bloque bloque = new Bloque(256);
    //Bloque* bloque = new Bloque(256);
	/*Bytes byte = new Bytes("hola");
	RegistroVariable registro = new RegistroVariable(hola);
	tamanioBloque tamanioTotal = bloque->getEspacioLibre();
	bool hayEspacio = bloque->tieneLugar();
	bloque->agregarRegistro(registro);
	tamanioBloque espacioLibre = bloque->getEspacioLibre();
	bool ok = if(tamanioTotal > espacioLibre);
    if (hayEspacio)
            cout << "Hello world!" << endl;
    //cout << "Probando svn!" << endl;
	//cout << "sisi funca" << endl;
    return 0;*/
};
